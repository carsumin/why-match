# 이유 있는 매칭 WhyMatch — SKILL.md

해커톤 팀 매칭 서비스 프로젝트입니다.
이 파일을 읽고 모든 작업에 아래 규칙을 반드시 따르세요.

---

## 프로젝트 개요

- **서비스명**: 이유 있는 매칭 (WhyMatch)
- **목적**: 해커톤 참가자들이 태그 기반 매칭 점수와 이유를 보고 팀을 구성할 수 있는 서비스
- **기술 스택**: Next.js 14 (App Router) + TypeScript + Tailwind CSS
- **배포**: Vercel

---

## 페이지 구조

| 경로 | 설명 | 우선순위 |
|------|------|----------|
| `/` | 메인 (카운트다운 배너, CTA 3개) | 필수 |
| `/hackathons` | 해커톤 목록 (카드, 마감 뱃지, 상태 필터) | 필수 |
| `/hackathons/[slug]` | 해커톤 상세 (7개 탭: 개요·평가·일정·상금·팀·제출·리더보드) | 필수 |
| `/camp` | 팀원 모집 (스와이프 탐색, 적합도 점수) | 필수 |
| `/camp/simulate` | 팀 빌딩 시뮬레이터 (합류 전후 점수 비교) | 확장 |
| `/messages` | 메시지함 (지원·수락·거절, 카카오톡 연결) | 선택 |
| `/rankings` | 랭킹 보드 (기간별 필터) | 필수 |
| `/profile/[id]` | 유저 프로필 (기술 스택, 포트폴리오) | 선택 |
| `/showcase` | 쇼케이스 갤러리 (수상작 아카이브) | 선택 |

---

## 핵심 비즈니스 로직

### 매칭 적합도 계산식
```
적합도 = (태그 일치 × 40%) + (역할 필요도 × 40%) + (활동 시간 × 20%)
```
- 점수는 0~100% 사이 숫자로 표현
- 점수와 함께 **이유 한 줄**을 반드시 함께 표시 (예: "React 기술이 일치합니다")
- 80% 이상: High Match, 60~79%: Medium Match, 60% 미만: Low Match

### 팀 빌딩 시뮬레이터
- **합류자 모드**: 내가 팀에 합류했을 때 팀 전체 적합도 변화 시뮬레이션
- **팀장 모드**: 특정 포지션 가상 추가 후 적합도 변화 확인

### 카운트다운
- 메인 페이지 상단 글로벌 배너에 가장 임박한 해커톤 마감 시간 표시
- 해커톤 목록: D-day 색상 뱃지 (빨강 = 임박, 초록 = 여유)
- 해커톤 상세: 실시간 초 단위 타이머

---

## 폴더 구조

```
src/
├── app/                        # Next.js App Router 페이지
│   ├── page.tsx                # 메인
│   ├── hackathons/
│   │   ├── page.tsx
│   │   └── [slug]/page.tsx
│   ├── camp/
│   │   ├── page.tsx
│   │   └── simulate/page.tsx
│   ├── messages/page.tsx
│   ├── rankings/page.tsx
│   ├── profile/[id]/page.tsx
│   └── showcase/page.tsx
├── components/
│   ├── ui/                     # 버튼, 카드, 뱃지 등 공통 UI
│   ├── layout/                 # 헤더, 푸터, 네비게이션
│   ├── matching/               # 매칭 점수 카드, 스와이프 컴포넌트
│   ├── hackathon/              # 해커톤 카드, 탭, 타이머
│   └── simulator/              # 팀 빌딩 시뮬레이터 UI
├── utils/
│   └── matching.ts             # 매칭 적합도 계산 로직
├── data/
│   ├── hackathons.ts           # 해커톤 더미 데이터
│   └── teams.ts                # 팀 더미 데이터
└── types/
    └── index.ts                # 공통 타입 정의
```

---

## 코딩 규칙

### 일반
- 모든 컴포넌트는 **함수형 컴포넌트**로 작성
- `'use client'`는 꼭 필요한 컴포넌트에만 사용 (인터랙션, 상태 필요한 경우)
- 변수명·함수명은 **camelCase**, 컴포넌트명은 **PascalCase**
- 주석은 **한국어**로 작성
- 타입은 `interface` 우선, 유니온 타입은 `type` 사용

### 스타일
- **Tailwind CSS**만 사용 (별도 CSS 파일 금지)
- 디자인 컬러 기준:
  - 주조색: 보라/인디고 계열 (`indigo-600`, `purple-600`)
  - 성공/일치: 초록 (`green-500`)
  - 경고/임박: 빨강 (`red-500`)
  - 배경: 연보라·연민트 그라데이션

### 컴포넌트
- props 타입은 항상 `interface`로 명시
- 더미 데이터는 `data/` 폴더에서 import해서 사용
- 재사용 가능한 UI는 반드시 `components/ui/`에 분리

---

## 개발 우선순위 (로드맵)

1. **기반 세팅** — 프로젝트 초기화, 폴더 구조, 더미 데이터
2. **필수 페이지** — 메인, 해커톤 목록·상세, 랭킹 UI 구현
3. **팀 모집·메시지** — 스와이프 탐색, 메시지함
4. **시뮬레이터** — 합류 전후 점수 변화, 모드 토글
5. **완성도 제고** — 반응형, 예외 처리
6. **마무리 문서** — README, 발표 준비

작업 요청 시 현재 어느 단계인지 알려주면 우선순위에 맞게 진행합니다.

---

## 주의사항

- 백엔드 없이 **더미 데이터**로만 동작하는 프론트엔드 프로젝트입니다
- 실제 API 연동 코드는 작성하지 않습니다
- 카카오톡 연결은 외부 링크(`href`) 처리로 대체합니다
